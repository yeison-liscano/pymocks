name: Publish to PyPI

on:
  push:
    branches:
      - main
    paths:
      - "pyproject.toml"
  workflow_dispatch:

permissions:
  contents: write
  id-token: write # Required for trusted publishing to PyPI

jobs:
  publish:
    runs-on: ubuntu-latest
    environment:
      name: pypi
    steps:
      - name: Checkout
        uses: actions/checkout@c2d88d3ecc89a9ef08eebf45d9637801dcee7eb5
        with:
          fetch-depth: 0 # Fetch all history for proper tagging

      - name: Extract version from pyproject.toml
        id: get_version
        run: |
          VERSION=$(grep -E '^version = ' pyproject.toml | sed -E 's/version = "(.*)"/\1/')
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Detected version: $VERSION"

      - name: Check if tag exists
        id: check_tag
        run: |
          if git rev-parse "v${{ steps.get_version.outputs.version }}" >/dev/null 2>&1; then
            echo "exists=true" >> $GITHUB_OUTPUT
            echo "Tag v${{ steps.get_version.outputs.version }} already exists"
          else
            echo "exists=false" >> $GITHUB_OUTPUT
            echo "Tag v${{ steps.get_version.outputs.version }} does not exist"
          fi

      - name: Install uv
        if: steps.check_tag.outputs.exists == 'false'
        uses: astral-sh/setup-uv@v6

      - name: Install Python 3.13
        if: steps.check_tag.outputs.exists == 'false'
        run: uv python install 3.13

      - name: Build
        if: steps.check_tag.outputs.exists == 'false'
        run: uv build

      # Smoke-test the built artifacts in an isolated env.
      # The script must run outside the project tree because tests/http_mcp/
      # would shadow the installed http_mcp package via sys.path[0].
      - name: Smoke test (wheel)
        if: steps.check_tag.outputs.exists == 'false'
        run: |
          cp tests/test_smoke.py /tmp/smoke_test.py
          uv run --isolated --no-project --with dist/*.whl python /tmp/smoke_test.py

      - name: Smoke test (source distribution)
        if: steps.check_tag.outputs.exists == 'false'
        run: uv run --isolated --no-project --with dist/*.tar.gz python /tmp/smoke_test.py

      - name: Publish to PyPI
        if: steps.check_tag.outputs.exists == 'false'
        run: uv publish

      - name: Create and push tag
        if: steps.check_tag.outputs.exists == 'false'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git tag -a "v${{ steps.get_version.outputs.version }}" -m "Release version ${{ steps.get_version.outputs.version }}"
          git push origin "v${{ steps.get_version.outputs.version }}"

      - name: Create GitHub Release
        if: steps.check_tag.outputs.exists == 'false'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "v${{ steps.get_version.outputs.version }}" \
            --title "Release ${{ steps.get_version.outputs.version }}" \
            --generate-notes